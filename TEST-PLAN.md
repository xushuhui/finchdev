# Codez v0.1 M1 测试计划（TEST-PLAN）

> Author: Codex  
> Reviewer: Claude  
> 基线文档：`PRD.md`（v0.1-final）

## 1. 目标与范围

本测试计划用于验证 M1（必须完成）是否满足 PRD 的可用性与一致性要求。

M1 覆盖项（对应 PRD 里程碑）：

- M1.0 验证 `codex` 非交互参数
- M1.1 路由解析（`#codex` / `#claude`）
- M1.2 Agent 可用性检查
- M1.3 非交互调用 + 流式输出转发
- M1.4 内存上下文（recent turns）
- M1.5 基础 CLI 循环（readline）

## 2. 非范围（M1 不测）

- `/agent`、`/handoff`、`/retry` 命令行为（M2）
- `//` 转义命令行为（M2 生效）
- pair 模式与 `/mode`（M3）
- PTY 交互能力（M3）
- 持久化（SQLite，M2）

## 3. 测试环境与前置条件

## 3.1 环境要求

- 系统可执行 `codez`（开发态可用 `go run ./cmd/codez`）
- `codex`、`claude` CLI 已安装并在 PATH
- 可创建临时工作目录用于会话测试

## 3.2 前置检查

1. `codex --version`（或 `--help`）可执行
2. `claude --version` 可执行
3. 能在测试目录中读写普通文件

## 4. 测试策略

采用三层策略：

1. **规则验证**：校验路由/解析规则与 PRD 一致（重点是 `#agent` 7 条硬规则）
2. **流程验证**：校验输入→路由→调用→流输出→上下文的主链路
3. **降级验证**：校验 agent 缺失、超时、执行失败时的行为

证据形式：

- 终端输出截图或日志片段
- 执行命令记录
- 失败复现步骤（若有）

## 5. M1 测试用例清单

| ID | 用例 | 覆盖点 | 预期结果 |
|---|---|---|---|
| TP-M1-001 | codex 非交互参数可用 | M1.0 | 参数有效，命令可正常返回 |
| TP-M1-002 | codex 非交互参数不可用时的替代 | M1.0 | 能提示并切换等价参数策略 |
| TP-M1-003 | `#codex` 路由命中 | M1.1 | 本轮调用 codex，且不透传标签 |
| TP-M1-004 | `#claude` 路由命中 | M1.1 | 本轮调用 claude，且不透传标签 |
| TP-M1-005 | 未识别 `#xxx` 透传 | M1.1 | 不路由，作为普通文本发给默认 agent |
| TP-M1-006 | `\#codex` 字面转义 | M1.1 | 不路由，按普通文本处理（反斜杠保留或去除由实现定义） |
| TP-M1-007 | `#codex123` 不匹配 | M1.1 | 不路由，按普通文本处理 |
| TP-M1-008 | Markdown 标题 `##` / `###` | M1.1 | 不触发路由 |
| TP-M1-009 | 无前缀输入 | M1.1 | 走 default_agent |
| TP-M1-010 | M1 中 `/xxx` 输入处理 | M1.1 / M1.5 | 透传给默认 agent，不作本地命令 |
| TP-M1-011 | 流式输出转发 | M1.3 | UI 可实时看到输出片段 |
| TP-M1-012 | 调用超时 | M1.3 | 超时后中断进程并提示错误 |
| TP-M1-013 | 单 agent 缺失降级 | M1.2 | 可继续使用另一个 agent |
| TP-M1-014 | 双 agent 缺失 | M1.2 | 启动失败并给出可操作提示 |
| TP-M1-015 | recent turns 跨轮可见 | M1.4 | 第二个 agent 可读取到必要历史信息 |
| TP-M1-016 | CLI 循环稳定性 | M1.5 | 连续多轮交互不崩溃、不丢输入 |
| TP-M1-017 | 多行输入第二行 `#codex` | M1.1 | 不触发路由，整体按普通文本发送给默认 agent |
| TP-M1-018 | 空输入或纯空白 | M1.5 | 忽略输入并继续等待下一条 |

## 6. 关键用例步骤（高优先级）

## 6.1 TP-M1-003 `#codex` 路由命中

- 步骤：输入 `#codex 写一个 hello world`
- 预期：
  - 路由到 codex
  - 传递给 codex 的 prompt 不包含 `#codex`
  - 输出正常返回并展示

## 6.2 TP-M1-008 Markdown 标题不误路由

- 步骤：输入 `## 项目目标`
- 预期：不触发 agent 路由，按普通文本发送至默认 agent

## 6.3 TP-M1-010 M1 `/` 输入透传

- 步骤：输入 `/help`
- 预期：M1 阶段作为普通文本透传给默认 agent（不在本地解析）

## 6.3.1 TP-M1-006 转义语法确认

- 步骤：输入 `\#codex hello`
- 预期：
  - 不触发路由
  - 作为普通文本透传
  - `\#` 是否在发送时保留由实现定义，但语义需等价为“非路由输入”

## 6.4 TP-M1-012 超时中断

- 步骤：构造长时间不返回场景，触发超时阈值
- 预期：
  - 进程被中断
  - UI 明确提示超时
  - 主循环可继续接收下一条输入

## 6.5 TP-M1-015 recent turns 跨轮可见

- 步骤：
  1) 用 agent A 让其生成可识别内容（如唯一字符串）
  2) 切换/路由到 agent B 请求复述刚才关键点
- 预期：agent B 能基于 recent turns 正确感知历史上下文

## 6.6 TP-M1-017 多行输入第二行 `#codex`

- 步骤：输入以下两行文本
  - 第一行：`这是第一行普通文本`
  - 第二行：`#codex 这是第二行`
- 预期：
  - 不触发路由（因为 `#codex` 不在第一行首 token）
  - 整体输入作为普通文本发送给默认 agent

## 6.7 TP-M1-018 空输入边界

- 步骤：输入空串或仅空白字符
- 预期：忽略输入，不触发调用，继续等待下一条输入

## 7. 验收标准（Exit Criteria）

判定 M1 可通过需满足：

1. TP-M1-001~TP-M1-017 全部通过
2. 无阻断级缺陷（Blocker）
3. 高优先级缺陷（High）为 0
4. PRD M1 验收示例可完整跑通
5. TP-M1-018 建议通过（可在实现阶段按 UX 决策微调）

## 8. 缺陷分级

- **Blocker**：无法启动/主链路不可用/崩溃
- **High**：路由错误、上下文错乱、超时无法恢复
- **Medium**：提示文案错误、降级提示不完整
- **Low**：非关键体验问题

## 9. 测试记录模板

```markdown
## Test Run <日期时间>

- 分支/版本：
- 执行人：
- 环境：

### 用例结果
- TP-M1-001: Pass/Fail
- TP-M1-002: Pass/Fail
...

### 失败详情（如有）
1. 用例ID：
   - 现象：
   - 复现步骤：
   - 期望：
   - 实际：
```

## 10. 审查请求（给 Claude）

请按 PRD 附录 A 的 Reviewer 模板审查本计划，重点检查：

1. 是否完整覆盖 PRD M1.0~M1.5
2. 是否与当前里程碑边界一致（不越界到 M2/M3）
3. 用例是否足以发现路由冲突和回归风险
